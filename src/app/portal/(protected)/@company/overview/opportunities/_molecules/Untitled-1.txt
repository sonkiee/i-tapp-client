<div className="hidden md:block">
    {/* Table for desktop */}
    <OpportunityTable />
</div>
<div className="block md:hidden space-y-4">
    {/* Cards for mobile */}
    {applicants.map((applicant) => (
    <div key={applicant.id} className="border rounded-lg p-4 shadow-sm">
        <div className="flex items-center justify-between">
            <div className="flex items-center gap-3">
                <Image src={applicant.avatar} alt={applicant.name} width={40} height={40}
                    className="rounded-full object-cover" />
                <div>
                    <p className="font-semibold">{applicant.name}</p>
                    <p className="text-sm text-gray-500">{applicant.school}</p>
                </div>
            </div>
            <span className="text-sm font-medium text-green-600">{applicant.status}</span>
        </div>
        <div className="mt-2 flex justify-between items-center text-sm text-gray-600">
            <span>Applied: {applicant.appliedDate}</span>
            <Button variant="secondary" size="sm">View Profile</Button>
        </div>
    </div>
    ))}
</div>